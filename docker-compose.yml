version: '3.8'

services:
  nl-to-sql:
    build: .
    container_name: nl-to-sql-engine
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - SQL_DATABASE=master
      - FLASK_ENV=production
    ports:
      - "5000:5000"
    restart: unless-stopped
